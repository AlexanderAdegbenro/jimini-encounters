/**
 * Jimini Health typography.
 * Clean sans-serif (platform default; load Inter via expo-font for custom branding).
 */
export const typography = {
  header: {
    fontWeight: '600' as const,
    fontSize: 17,
  },
  body: {
    fontSize: 16,
  },
  secondary: {
    fontSize: 14,
  },
  caption: {
    fontSize: 12,
  },
};
